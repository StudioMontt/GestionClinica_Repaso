package cl.awakelab.repaso.algoritms;

import java.lang.reflect.Array;
import java.util.List;

public class CalculoTest {
/*
    @Test
    public void testCalculoEdad() {
        CalculoDescuento descuento = new CalculoEdad();
        List<Double> descuentos = Array.asList(4,37,25,29,10,57);
        double resultadoEsperado = [0.05, 0.1, 0.0, 0.0, 0.03, 0.1];
        double resultadoObtenido = descuento.descuento(descuentos);
        assertEquals(resultadoEsperado, resultadoObtenido);
    }
*/

}
